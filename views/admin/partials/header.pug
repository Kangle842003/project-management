header
  .row.align-items-center
    .col-6
      h1 🚀 Tech Admin Dashboard

    .col-6
      .d-flex.justify-content-end.align-items-center.gap-3
        a.text-decoration-none.text-dark.text-end(
          class ="btn btn-outline-info"
          href=`${prefixAdmin}/my-account`
        )
          h6.mb-0 #{infoAccount.fullName}
          small.text-muted #{roleAccount.title}

        a.btn.btn-warning(
          href=`${prefixAdmin}/auth/logout`
        ) Đăng xuất