extends ../dashboard/index.pug
include ../../mixins/alert.pug
block main
  if(roleAccount.permissions.includes("role_create"))
    +error-flash(4000)
    .container.mt-4
      .card.shadow-sm.border-0
        .card-header.bg-primary.text-white
          h4.mb-0 Them nhom quyen

        .card-body
          form(
            action=`${prefixAdmin}/roles/create`
            method="POST"
          )
            .mb-3
              label.form-label(for="title") TIÊU ĐỀ
              input.form-control(
                id="title"
                name="title"
                type="text"
                placeholder="Nhập tiêu đề danh mục..."
                required
              )
            .mb-3
              label.form-label(for="desc") MÔ TẢ
              textarea.form-control(
                id="desc"
                name="description"
                rows="4"
                placeholder="Nhập mô tả danh mục ..."
                class ="timymce"
              )
            button.btn.btn-primary.me-2(type="submit") THÊM MỚI
            
            a.btn.btn-outline-secondary(
              href=`${prefixAdmin}/roles`
            )
              i.fa-solid.fa-arrow-left.me-2
              | QUAY LẠI

  script(src="/tinymce/tinymce.min.js") 
  script(src="/admin/js/tinymce-config.js") 